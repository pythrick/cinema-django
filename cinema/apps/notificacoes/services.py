

def enviar_whatsapp(numero, msg):
    print(f"Mensagem `{msg}` enviada para número: {numero} via WhatApp.")


def enviar_email(para, titulo, mensagem):
    print(f"Email com `{titulo}` enviado para os seguintes emails {para} com a seguinte mensagem: {mensagem}")
